import s from "./HomePage.module.css";

const HomePage = () => {
    return (
        <div className={s.container}>
            <h1 className={s.title}>Welcome to the Contact Book App!</h1>
        </div>
    );
};

export default HomePage;